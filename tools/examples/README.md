Platformio has its own python intepreter!

```
which -a pyhton
python -m pip install pymodbus
```

adjust shebang hash in pyton file:
`#!/home/stif/.platformio/penv/bin/python` or `#!/usr/bin/python`
